#include <stdio.h>

//made by: 2cache

int
main(void)
{
    double x, y;
    scanf("%lf %lf", &x, &y);

    if ((y > 7 || y < 1) || (x < 2 || x > 5)) {
        printf("0\n");
    } else if (y < x - 2) {
        printf("0\n");
    } else {
        printf("1\n");
    }
}
