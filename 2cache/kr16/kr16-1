#include <stdio.h>
#include <stdlib.h>
#include <dirent.h>
#include <sys/types.h>
#include <sys/stat.h>
#include <string.h>
#include <limits.h>
#include <unistd.h>
#include <fcntl.h>
#include <errno.h>
#include <sys/wait.h>
#include <signal.h>
#include <sys/time.h>
#include <stdio.h>
#include <pthread.h>
#include <unistd.h>
#include <unistd.h>

//made by: dead bit

int main(void) {
	unsigned long long res = 0, buf = 0, mask = 3;
	while (fscanf(stdin, "%llx", &buf) == 1) {
		mask = 3;
		unsigned long long cur = 0;
		for (int i = 0; i < (sizeof(buf)*CHAR_BIT); i+=2) {
			if (!(mask & buf)) {
				cur++;
			}
			buf >>= 2;
		}
		res += cur;
	}
	fprintf(stdout, "%lld", res);
}
